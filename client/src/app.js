const UserView = require('./views/user_login_view.js');
const ChatView = require('./views/chat_view.js');

document.addEventListener('DOMContentLoaded', () =>{

  const userView = new UserView();
  const chatView = new ChatView();

})
